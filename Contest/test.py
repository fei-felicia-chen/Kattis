class Node:
    def __init__(self,letter):
        self.letter=letter
        self.children= list()
        
def h(tree, used, maxv):
    

def tree(n):
    tree = {}
    lst = [i for i in range(n)]
    for i in range(n):
        tree.add(Node(lst[i]))
        for j in range(1,n):
            tree.Node(lst.pop())

print(tree(3))